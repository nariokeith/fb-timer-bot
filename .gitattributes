# Windows reads these; they must arrive with CRLF however they were
# committed. A .bat with LF endings is a documented source of odd
# cmd.exe behaviour, and these are edited from a Mac.
*.bat  text eol=crlf
*.ps1  text eol=crlf
